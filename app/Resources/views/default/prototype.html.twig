{% block body %}
{{ form_start(form) }}
{{ form_widget(form) }}
{{ form_end(form) }}

   {# {% for value in item %} #}
        <div>
            <h1>{{ item.title }}</h1>
            <p>{{ item.id }} {{ item.price }} {{ item.description }}</p>
        </div>
   {# {% endfor %} #}
{% endblock %}
